[pytest]
minversion = 6.0
addopts = -q --html=artifacts/report.html --self-contained-html
testpaths = tests
markers =
    smoke: Quick smoke tests that exercise critical paths (fast, high-level).
    sanity: Sanity tests — a slightly broader set that verifies core functionality.